# General
alias ..="cd .."
alias ...="cd ../.."
alias c="clear"
alias h="history"
alias l="ls -alh"
alias p="pwd"
alias cpath='pwd | tr -d "\r\n" | pbcopy |echo "Current path copied to clipboard."'
alias o="open"
alias m="mate ."
alias home="cd ~"
alias tree="find . -print | sed -e 's;[^/]*/;|____;g;s;____|; |;g'"
alias bashm="mate ~/.bashrc"
alias bashs="source ~/.bashrc"

# Global Colors
NORMAL=$'\e[00m'
GREEN=$'\e[0;32m'
RED=$'\e[0;31m'
BLUE=$'\e[0;34m'
WHITE=$'\e[1;37m'
BLACK=$'\e[0;30m'
YELLOW=$'\e[0;33m'
PURPLE=$'\e[0;35m'
CYAN=$'\e[0;36m'
GRAY=$'\e[1;30m'
PINK=$'\e[37;1;35m'
ORANGE=$'\e[33;40m'
LIGHT_BLUE=$'\e[1;34m'
LIGHT_GREEN=$'\e[1;32m'
LIGHT_CYAN=$'\e[1;36m'
LIGHT_RED=$'\e[1;31m'
LIGHT_PURPLE=$'\e[1;35m'
LIGHT_YELLOW=$'\e[1;33m'
LIGHT_GRAY=$'\e[0;37m'

# Terminal Colors

# The color designators:
#	 a  black
#	 b  red
#	 c  green
#	 d  brown
#	 e  blue
#	 f  magenta
#	 g  cyan
#	 h  light grey
#	 A  bold black, usually shows up as dark grey
#	 B  bold red
#	 C  bold green
#	 D  bold brown, usually shows up as yellow
#	 E  bold blue
#	 F  bold magenta
#	 G  bold cyan
#	 H  bold light grey; looks like bright white
#	 x  default foreground or background

# The order of the attributes are as follows:
#  1.   directory
#  2.   symbolic link
#  3.   socket
#  4.   pipe
#  5.   executable
#  6.   block special
#  7.   character special
#  8.   executable with setuid bit set
#  9.   executable with setgid bit set
#  10.  directory writable to others, with sticky bit
#  11.  directory writable to others, without sticky bit

# The default is "exfxcxdxbxegedabagacad"
export CLICOLOR=1
export LSCOLORS=cxfxcxdxbxegebabagacad

# Command Prompt
PS1="[\t][\u@\h:\w]> "

# Git (http://git-scm.com)
alias gst="git status -sb"
alias gg="git grep"
alias gl="git log --oneline --decorate"
alias glc="git shortlog -sne"
alias gld="git log --graph --pretty=format:'%Cred%h%Creset %an: %s - %Creset %C(yellow)%d%Creset %Cgreen(%cr)%Creset' --abbrev-commit --date=relative"
alias gd="git diff"
alias gdw="git diff --word-diff"
alias gb="git branch -v"
alias gm="git merge"
alias gcl="git clone"
alias gch="git checkout"
alias ga="git add"
alias gall="git add ."
alias gco="git commit"
alias gca="git commit -a"
alias gcam="git commit -a -m"
alias gf="git fetch"
alias gpu="git pull"
alias gp="git push"
alias gpo="git push origin"
alias gpp="git push production master"
alias gps="git push stage stage:master"
alias gtag="git tag"
alias gtags="git push --tags"

# Apache
alias apachec="mate /etc/apache2/httpd.conf"
alias apachev="mate /private/etc/apache2/extra/httpd-vhosts.conf"
alias apaches="mate /private/etc/apache2/extra/httpd-ssl.conf"
alias apacher="sudo /usr/sbin/apachectl restart"

# Ruby (http://www.ruby-lang.org)
export RUBYOPT=-w
alias rd="rdoc -a -o _doc"

# Ruby on Rails (http://rubyonrails.org)
function sc {
  if [ -e script/rails ]; then
    script/rails console $@
  else
    script/console $@
  fi
}

function ss {
  if [ -e script/rails ]; then
    script/rails server $@
  else
    script/server $@
  fi
}

function sg {
  if [ -e script/rails ]; then
    script/rails generate $@
  else
    script/generate $@
  fi
}

alias scs="sc --sandbox"
alias sgc="sg controller"
alias sgm="sg model"
alias sgs="sg scaffold"
alias rdbd="rake db:drop"
alias rdbc="rake db:create"
alias rdbm="rake db:migrate"
alias taild="tail -f log/development.log"
alias tailt="tail -f log/test.log"
alias cdl="> ./log/development.log"
alias ctl="> ./log/test.log"
alias cpl="> ./log/production.log"
alias cbl="> ./log/bullet.log"
alias clogs="cdl; ctl; cpl; cbl"
alias res="rake restart"
alias debug="rdebug -c"
alias rpaths="rake routes | sed -e "1d" -e "s,^[^/]*,,g" | awk '{print $1}' | sort | uniq"
alias erd="rake erd attributes=primary_keys,foreign_keys,timestamps,inheritance,content inheritance=true orientation=vertical filename=doc/design/models"
alias rmodels="rake doc:diagram:models"
alias rcontrollers="rake doc:diagram:controllers"
alias rdemo="rails new demo -d=sqlite3 -J -T -f -m https://github.com/bkuhlmann/rails_setup_template/raw/master/template.rb"

# Bundler (http://gembundler.com)
alias ber="bundle exec rspec"
alias bec="bundle exec cucumber"
alias rebundle="rm -f Gemfile.lock; bundle check; bundle package"

# Default Editor (for use with open_gem: http://github.com/adamsanderson/open_gem)
export EDITOR=mate

# Ruby Version Manager (http://rvm.beginrescueend.com)
[[ -s "$HOME/.rvm/scripts/rvm" ]] && . "$HOME/.rvm/scripts/rvm"
if [[ -s .rvmrc ]] ; then source .rvmrc ; fi # Fixes RVM config failure when creating new terminal tabs (example: iTerm).
